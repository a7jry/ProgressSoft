version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: fxwarehouse-db
    environment:
      POSTGRES_DB: fxwarehouse
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
